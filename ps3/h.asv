function h_output = h(gammaf, lambda, rhopi, rhox)
   

   h1 = (rhox - rhox*rhopi*gammaf)/(rhox*gammaf + lambda);
   h2 = (rhopi - rhopi^2*gammaf + gammaf - 1)/(rhox*gammaf + lambda);
   h3 = rhox;
   h4 = rhopi;

   h_output = [h1 h2 h3 h4]';
end